#파이썬다운 방식(내장 함수 사용하면 쉽다.)
def reverseString(self, s : List[str]) -> None:
    s.reverse() #리스트를 뒤집는 경우
    
    
 # 문자열을 사용하는 경우가 플랫폼마다 다르다 즉 s= s[::-1]이 성립되어야 하지만 리트코드에서는 오류가 발생하며 s[:] = s[::-1]로 해야 오류가 해결된다. 이는 플랫폼마다 다르므로 확인이 필요
 
 문자열 문제 잘 풀려면 정규식을 다시배워야한다.
