백트래킹,,,백트래킹 = DFS가 아니다! 헷갈려하지 말자!
백트래킹이 훨씬 넓은 범위의 개념이다.

depth++
depth+1은 완전히 다른 개념이다.

depth++은 depth 변수의 값 자체가 1증가하기 때문에 재귀에서 빠져나오더라도 증가된 값은 그대로 유지가 된다.
반면 depth + 1 은 자바 내부에서 임시의로 depth+1 값을 복사하여 해당 값을 사용하기 때문에 재귀에서 삐져나오게 되면
depth+1이 없어지게 된다.

즉 남아있는 것인지 없는 것인지가 매우 중요하다.
